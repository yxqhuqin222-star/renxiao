# Renxiao 项目协作规则

## 数据分析与日报

- 当用户要求分析本项目数据、核对指标、解释异常、生成日报/周报/月报时，默认使用本机 skill：`/Users/kityhello/.codex/skills/huashu-excel/SKILL.md`。
- 先确认当前数据来源、日期窗口、表结构和已有未提交改动；不要覆盖用户上传文件、数据库或静态导出产物。
- 涉及成本、转化率、人效、聚合值等业务指标时，必须先说明口径；聚合指标从源分子/分母重算，不对展示行的比例做平均。
- 对每天更新的“情况”，默认基于项目当前可用的最新数据，输出：最新日期、核心指标变化、异常点、可能原因、需要用户决策/补数据的事项。
- 若发现异常增长、100%/0%、空分母、日期断档、分类缺失或历史库未刷新，先按数据问题排查，不能直接当业务结论。
- 普通小范围查看采用轻量验证；涉及口径变更、重新计算数据库、发布静态页或对外汇报时，按风险做充分验证。

## 运行与发布边界

- 本项目当前发布范围以 README 为准：本地 Flask 服务用于完整交互，GitHub/GitHub Pages 用于只读快照；不要默认恢复或发布到 Vercel。
- 修改运行中服务后，要重启对应本地服务并检查目标端点；只改代码不等于已生效。
